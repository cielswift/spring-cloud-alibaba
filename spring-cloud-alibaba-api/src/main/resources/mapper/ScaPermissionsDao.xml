<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.a.dao.ScaPermissionsDao">

    <resultMap type="com.a.entity.ScaPermissions" id="ScaPermissionsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="NAME" jdbcType="VARCHAR"/>
        <result property="deleted" column="deleted" jdbcType="OTHER"/>
        <result property="createDate" column="create_date" jdbcType="TIMESTAMP"/>
        <result property="updateDate" column="update_date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ScaPermissionsMap">
        select
          id, NAME, deleted, create_date, update_date
        from spring_alibaba.sca_permissions
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ScaPermissionsMap">
        select
          id, NAME, deleted, create_date, update_date
        from spring_alibaba.sca_permissions
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ScaPermissionsMap">
        select
          id, NAME, deleted, create_date, update_date
        from spring_alibaba.sca_permissions
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and NAME = #{name}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
            <if test="createDate != null">
                and create_date = #{createDate}
            </if>
            <if test="updateDate != null">
                and update_date = #{updateDate}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into spring_alibaba.sca_permissions(NAME, deleted, create_date, update_date)
        values (#{name}, #{deleted}, #{createDate}, #{updateDate})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update spring_alibaba.sca_permissions
        <set>
            <if test="name != null and name != ''">
                NAME = #{name},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
            <if test="createDate != null">
                create_date = #{createDate},
            </if>
            <if test="updateDate != null">
                update_date = #{updateDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from spring_alibaba.sca_permissions where id = #{id}
    </delete>

</mapper>